# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'InitialDriver' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for InitialDriver
  pod 'RxSwift',    '~> 4.0'
  pod 'RxCocoa',    '~> 4.0'
  pod 'Mapbox-iOS-SDK', '~> 3.7'
  pod 'SwiftyUserDefaults'
  pod 'Alamofire', '~> 4.4'
  pod 'SideMenu'

  target 'InitialDriverTests' do
    inherit! :search_paths
    # Pods for testing
    pod 'RxBlocking', '~> 4.0'
    pod 'RxTest',     '~> 4.0'
  end

  target 'InitialDriverUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
